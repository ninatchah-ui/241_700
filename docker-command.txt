//Show all containers (default shoe just runninh)
docker ps --all

docker ps


docker run hello-world

//stop containers
docker stop containers

//pull an image from a registry
docker pull <image_name>

docker run containers ping google name

docker create
docker start <container id>

docker exec -it a043d77e1d72 sh
docker exec -it a043d77e1d72 bash

usr/share/nginx/html
ls
pwd
ls

//ดูโค้ด
cat index.html

//กำหนดเส้นทาง port
docker run -p <80:80> <image_name>
docker run -p <9000:80> <image_name>